<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>

  <body>
    <h1> <%= @event.title %></h1>
    <p><%= t('events.invitation_text') %> <%= @user_from.email %></p>
    <ul>
      <li><%= t('events.date_from') %>: <%= @event.date_from %></li>
      <li><%= t('events.date_to') %>:  <%= @event.date_to %></li>
      <li><%= t('events.where') %>: <%= @event.place %></li>
    </ul>
  </body>
</html>
